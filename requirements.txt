# requirements.txt
# PyGoat v2 — Core application dependencies

# ── Django Core ────────────────────────────────────────────────────────────────
Django==4.2.16
djangorestframework==3.15.2
django-allauth==0.63.6
django-crispy-forms==2.3
crispy-bootstrap4==2024.1
whitenoise==6.7.0
gunicorn==22.0.0
psycopg2-binary==2.9.9

# ── Lab dependencies (used by introduction/ legacy labs) ─────────────────────
# These were in the original PyGoat requirements and are kept for lab logic
PyJWT==2.9.0
argon2-cffi==23.1.0
Pillow==10.4.0
requests==2.32.3
pyyaml==6.0.2

# ── Flask (lab microservices) ─────────────────────────────────────────────────
Flask==3.0.3
Flask-SQLAlchemy==3.1.1

# ── Docker SDK (LabController) ────────────────────────────────────────────────
docker==7.1.0


# requirements-dev.txt
# Development and test dependencies (not installed in production)

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-django==4.9.0
pytest-cov==5.0.0
playwright==1.47.0
pytest-playwright==0.5.1

# ── Linting / Type checking ───────────────────────────────────────────────────
flake8==7.1.1
ruff==0.6.9
mypy==1.11.2
django-stubs==5.1.0
djangorestframework-stubs==3.15.0
